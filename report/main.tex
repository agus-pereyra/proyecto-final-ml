\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage[spanish]{babel}
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{float}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{siunitx}

\begin{document}

\title{Predicción de Etapas del Sueño sobre señales Fisiológicas capturadas con SmartWatch\\
% {\footnotesize \textsuperscript{*}Note: Sub-titles are not captured for https://ieeexplore.ieee.org  and
% should not be used}
}

\author{\IEEEauthorblockN{Agustín Adrián Pereyra}
\IEEEauthorblockA{\textit{Departamento de Ingeniería} \\
\textit{Universidad de San Andrés}\\
Buenos Aires, Argentina \\
\url{apereyra@udesa.edu.ar}}
\and
\IEEEauthorblockN{Agustín Patruno}
\IEEEauthorblockA{\textit{Departamento de Ingeniería} \\
\textit{Universidad de San Andrés}\\
Buenos Aires, Argentina \\
\url{apatruno@udesa.edu.ar}}
}

\maketitle

\begin{abstract}
\end{abstract}

\begin{IEEEkeywords}
sleep staging, recurrent neural networks, heart rate variability, accelerometry, EEG, deep learning
\end{IEEEkeywords}

\section{Introducción}

Frente a la polisomnografía --costosa y confinada al laboratorio--, los dispositivos \emph{smart-watch} son capaces de registrar IHR (\emph{Instantaneous Heart Rate}) y acelerometría de forma continua y no invasiva, habilitando el monitoreo domiciliario.

La \emph{estadificación del sueño} (\emph{sleep staging}) --asignar una etapa a cada época de $30$\,s de registro, según el estándar clínico AASM-- es central para el diagnóstico de trastornos como la apnea o el insomnio, pero hoy depende de la polisomnografía y de la anotación manual de un experto, lo que la vuelve cara e incómoda. Lograrla a partir de señales de muñeca abriría la puerta a un monitoreo accesible y prolongado en el tiempo.

Abordamos la tarea como un problema de \textbf{clasificación multiclase supervisada}. La entrada de nuestro algoritmo es la IHR y la acelerometría triaxial de una época de $30$\,s (o su secuencia a lo largo de la noche), y utilizamos modelos de aprendizaje automático desde un \emph{baseline} tabular (XGBoost, MLP) hasta modelos secuenciales que utilizan la señal cruda (BiLSTM y un híbrido CNN--BiLSTM) para predecir la etapa del sueño. Como referencia se emplean las etiquetas de un experto (\emph{ground-truth}) y una referencia secundaria (y como nuestra competencia) las etiquetas de un dispositivo Dreem Headband, basado en EEG (electroencefalograma). El eje del trabajo es comparar esta progresión de modelos para cuantificar el aporte del \textbf{contexto temporal inter-época} --la dependencia entre etapas contiguas-- frente a la clasificación de cada época de forma aislada.

\section{Conjunto de Datos y Características}


PhysioNet BIDSleep~\cite{bidsleep} recopila señales fisiológicas tales como la frecuencia cardíaca instantánea (IHR) y tres ejes de acelerometría medidas con un Apple Watch a lo largo de 253 noches de 47 adultos sanos. Cada lapso de 30 segundos se encuentra etiquetado por dos fuentes diferentes (\emph{expert} y \emph{dreem}) con una etapa del sueño: 
\begin{enumerate}[start=0]
    \item \textbf{Wake} (Despierto)
    \item \textbf{N1} (Sueño Ligero): etapa más superficial y la entrada al sueño. Suele ser la clase más dificil de detectar dado que es breve e inestable.
    \item \textbf{N2} (Sueño Ligero consolidado): sujeto dormido de forma estable. Es la etapa más frecuente.
    \item \textbf{N3} (Sueño profundo): el sueño mas reparador. En él cuesta mucho despertar al paciente. Debería mostrar IHR mínimo y cuerpo inmóbil 
    \item \textbf{REM} (\emph{Rapid Eye Movement}): también llamado ``sueño paradójico''. El EEG se parece al de vigilia pero el paciente está profundamente dormido, involucrando IHR irregular.
\end{enumerate}

Podemos observar un ejemplo de todos los datos contenidos en una noche en la Figura~\ref{fig:night}.

\begin{figure}[h]
    \centering
    \includegraphics[width=\linewidth]{figures/night-overview-0-1.png}
    \caption{Ejemplo de la entrada del algoritmo: señales (IHR y acelerometría) e hipnograma de una noche completa (Paciente 00, Noche 1).}
    \label{fig:night}
\end{figure} 

A la hora de particionar el conjunto para el entrenamiento se realiza \textbf{por paciente} evitando así \textit{leakage} y midiendo la generalización a sujetos nuevos. 
Se emplean dos representaciones: una \textbf{tabular} de $122$ \textit{features} por época y la
\textbf{señal cruda} $150\times4$ en secuencias por noche, ambas estandarizadas con estadísticos de
\emph{train}; los modelos densos imputan a la mediana los \texttt{NaN} de borde. El IHR se muestrea a
$\approx\!0.2$\,Hz (Nyquist $\approx\!0.1$\,Hz), por debajo de las bandas LF/HF ($0.04$--$0.4$\,Hz),
que resultan inobservables.

Antes de modelar se aplica un control de calidad noche a noche. Sobre cada noche se computan
\textit{gaps} temporales en IHR y acelerometría ($>60$s entre muestras), duración de la señal y
fracción de acelerometría inválida ($|\lVert a \rVert - 1| > 0.5g$), y se define la
\textbf{ventana válida} como la intersección entre el rango etiquetado y el de IHR \emph{continua}.
Los desajustes se clasifican en \textit{signal\_excess} (señal sin etiqueta, se recorta),
\textit{label\_trunc} (etiqueta sin señal, se recorta) e \textit{internal\_gap} (huecos $>10$ min).
Estos últimos, verificados como \textit{dropouts} reales del sensor, se resuelven recortando la
cola si el prefijo limpio supera el 60\% o descartando la noche. Todo se aplica en memoria, sin
alterar los archivos originales.

\section{Metodología}

\subsection{Métricas de Rendimiento}

Dado que se tienen dos criterios para tomar como \textit{ground-truth} (etiquetas del experto y del dispositivo Dreem) 
usamos el \textbf{coeficiente Cohen's Kappa} \cite{cohen1960coefficient} para cuantificar cuán de acuerdo están entre sí.
\cite{landis1977measurement}
\begin{equation}
    \kappa = \frac{p_o - p_e}{1 - p_e}
    \label{eq:cohen-kappa}
\end{equation}

El desempeño de clasificación se mide con el \textbf{F1-score macro} —media del F1 por clase, que
al pesar todas las etapas por igual es la métrica más honesta bajo el desbalance—, complementado
con la \textbf{accuracy} global y la \textbf{matriz de confusión}, que localiza entre qué etapas
ocurren los errores.

\subsection{Elección de modelos}

Como \textbf{baseline} se usa \textbf{XGBoost} sobre \textit{features} por época: modelo tabular
robusto, que maneja valores faltantes de forma nativa y da importancia interpretable. Las
\textit{features} resumen IHR y HRV en el dominio temporal (RMSSD, pNN50, etc.) y la acelerometría
sobre la magnitud $\lVert a \rVert$ —invariante a la orientación del reloj— e incorporan contexto
inter-época (\textit{lags}, \textit{leads}, promedios móviles), clave en \textit{sleep staging}.
Sobre esas mismas \textit{features} entrenamos además un \textbf{perceptrón multicapa (MLP)}, que
contrasta una red densa con los árboles con idéntica entrada (imputando los \texttt{NaN} de borde
y estandarizando, pasos que XGBoost no requiere).

Frente a ellos, una \textbf{CNN 1D} opera sobre la señal cruda, con la hipótesis de aprender la
dinámica temporal —incluida la variabilidad de alta frecuencia de la acelerometría que las
\textit{features} manuales descartan— sin \textit{feature engineering}. Cada época se lleva a un
tensor $150 \times 4$ (IHR interpolada y tres estadísticos por bin de $0.2$s de la magnitud
invariante: media, desvío y ENMO), que tres bloques convolucionales
(Conv1D--BatchNorm--ReLU--\textit{pooling}) más \textit{global average pooling} y un clasificador
denso mapean a las cinco etapas.

Los modelos anteriores clasifican cada época de forma aislada, ignorando que el sueño es una
secuencia con fuerte estructura temporal. Para capturarla usamos una \textbf{LSTM bidireccional
(BiLSTM)}~\cite{hochreiter1997lstm,schuster1997bilstm} \emph{many-to-many}: procesa la noche
completa como secuencia y emite una etapa por época. En cada paso $t$, la celda LSTM regula un
estado de memoria $c_t$ mediante tres compuertas —entrada $i_t$, olvido $f_t$ y salida $o_t$—:
\begin{equation}
\begin{aligned}
(i_t, f_t, o_t) &= \sigma\!\big(W_{\{i,f,o\}}\, x_t + U_{\{i,f,o\}}\, h_{t-1} + b_{\{i,f,o\}}\big)\\
\tilde{c}_t &= \tanh(W_c x_t + U_c h_{t-1} + b_c)\\
c_t &= f_t \odot c_{t-1} + i_t \odot \tilde{c}_t, \quad h_t = o_t \odot \tanh(c_t)
\end{aligned}
\label{eq:lstm}
\end{equation}
donde $\sigma$ es la sigmoide y $\odot$ el producto elemento a elemento. La variante
\textbf{bidireccional} concatena una pasada hacia adelante y otra hacia atrás,
$h_t = [\,\overrightarrow{h}_t \,;\, \overleftarrow{h}_t\,]$, de modo que cada época integra
contexto pasado \emph{y} futuro (en \textit{sleep staging} \emph{offline} se dispone de la noche
completa); un cabezal lineal da la predicción por época $\hat{y}_t = \mathrm{softmax}(W h_t + b)$.

Por último, un \textbf{híbrido CNN--LSTM}, en la línea de SLAMSS~\cite{SLAMSS2023}, entrena ambos
componentes \emph{end-to-end}: la CNN 1D actúa como \emph{encoder} intra-época —reemplaza la
entrada $x_t$ de la BiLSTM por el vector que aprende de la señal cruda de cada época— y la BiLSTM
modela la dinámica inter-época sobre esa secuencia.

Todos los modelos neuronales se entrenan minimizando la \textit{cross-entropy} ponderada por
clase, $\mathcal{L} = -\sum_c w_c\, y_c \log \hat{y}_c$ con $w_c \propto 1/f_c$, para compensar el
desbalance. En todos los casos la partición \textbf{agrupa por paciente}, para evitar
\textit{leakage} y medir la generalización a sujetos nuevos, descartando \textit{Unknown}.

\section{Resultados}

\subsection{Análisis exploratorio y calidad de datos}

La distribución de clases está fuertemente desbalanceada: $N_2$ ($37.5\%$) y REM ($25.9\%$)
concentran la mayor parte de las épocas, seguidas de $N_3$ ($18.5\%$) y Wake ($11.0\%$), mientras
que $N_1$ es marginal ($7.1\%$). Este desbalance justifica evaluar con $F1_{Macro}$, que pondera
cada clase por igual, y compensar la frecuencia de clase durante el entrenamiento.

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{figures/raw-vs-clean-20-1.png}
\caption{Ejemplo de procesamiento de una noche (Paciente 20, noche 1). La ventana válida (fondo verde) queda delimitada por el final de la continuidad de la acelerometría (IHR continúa) hasta las 7.94hs conservando 949/1282 épocas.}
\label{fig:problematic}
\end{figure}

El control de calidad (Fig.~\ref{fig:problematic}) revela que \textbf{229 de 253 noches} (90.5\%)
requieren alguna modificación —en su mayoría el trivial \textit{signal\_excess}— y que, al acotar
la ventana válida por ambas señales (IHR y acelerometría), sólo \textbf{1} noche conserva un
\textit{internal\_gap} genuino (P42 N4), que se descarta entera. La figura ilustra el procesamiento en una noche con \textit{gaps} (P20 N1):
la ventana válida (verde) conserva el tramo aprovechable mientras se recorta la cola donde la
acelerometría se interrumpe y aparecen \textit{gaps} de señal (rojo), y el hipnograma procesado descarta
esas épocas del final.
\vspace{-1.2em}

El acuerdo entre las dos fuentes de etiquetado (Ec.~\ref{eq:cohen-kappa}) fija el techo de la
tarea: $\kappa = 0.74$ global, máximo en $N_3$ ($0.90$) y REM ($0.85$) y mínimo en $N_1$ ($0.13$),
la etapa más fugaz y ambigua. Su matriz de
confusión (Fig.~\ref{fig:cm-expert-dreem}) muestra que el desacuerdo entre el experto y Dreem se
concentra en etapas contiguas ($N_1$ vs.\ $N_2$ y Wake), las transiciones intrínsecamente más
ambiguas de discriminar.

\subsection{Clasificación}

Los hiperparámetros de cada modelo se ajustan mediante \textbf{optimización bayesiana}: en lugar de
una búsqueda exhaustiva (\textit{grid search}) o aleatoria, se construye un modelo sustituto
probabilístico de la métrica objetivo en función de los hiperparámetros y, en cada iteración, una
función de adquisición propone la configuración más prometedora, equilibrando exploración y
explotación. El objetivo optimizado es el $F1_{Macro}$ sobre la partición de
\emph{validación} —\textbf{agrupada por paciente}, igual que el resto del \textit{pipeline}, para no
introducir \textit{leakage} en la selección— y la configuración ganadora se reentrena sobre
\emph{train} y se reporta una única vez sobre \emph{test}. El espacio de búsqueda por modelo, el
número de iteraciones y los valores finales seleccionados se detallan en la Tabla~\ref{tab:resultados}.

El baseline XGBoost alcanza $F1_{Macro} = 0.47$ y $\kappa = 0.38$
(Ec.~\ref{eq:cohen-kappa}) frente al experto, con \textit{accuracy} del 55\%
(Tabla~\ref{tab:resultados}). Pese al desbalance, capta señal discriminativa en las cinco clases,
aunque lejos del techo humano ($\kappa = 0.74$); los errores se concentran entre etapas adyacentes
($N_1$ vs.\ $N_2$ y REM).

\begin{table}[!ht]
\caption{Rendimiento en test (5 clases) frente al etiquetado del experto.}
\label{tab:resultados}
\centering
\begin{tabular}{lccc}
\hline
\textbf{Modelo} & \textbf{$F1_{acro}$} & \textbf{Cohen's $\kappa$} & \textbf{Accuracy} \\
\hline
Dreem Label & --- & 0.74 &  -- \\
XGBoost & 0.4707 & 0.3776 & \\
MLP & & \\
BiLSTM & 0.559 & 0.443\\
Híbrido & & \\
\hline
\end{tabular}
\end{table}

\section{Conclusión}

Presentamos un \textit{pipeline} de estadificación del sueño sobre señales de muñeca, del control
de calidad a la clasificación. El acuerdo experto--Dreem ($\kappa = 0.74$) fija un techo realista
y el baseline tabular ($F1_{Macro}=0.47$, $\kappa = 0.38$) supera al azar pese al desbalance. La
principal limitación del enfoque convolucional es la falta de contexto inter-época —clave en el
tabular—, lo que motiva extenderlo a modelos recurrentes (LSTM/BiLSTM) sobre secuencias de épocas.

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
